# Payments AI

Use the `payments-quickstart` and `checkout-customization` skills in this extension. They call the bundled Payments AI MCP server (`payments-ai`).

On first MCP tool call, complete the OAuth browser flow. If tools are missing, reload Gemini CLI after `gemini extensions install`.
